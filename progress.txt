# Ralph Progress Log
Started: Tue Mar  3 10:23:37 AM CET 2026
---

## Codebase Patterns

- `ownerUserId` on skill table is now nullable (system-default vault skills have no user owner)
- output zod schema for skill uses `ownerUserId: z.string().nullable()`
- schema files live in `packages/db/src/schema/`, barrel-exported from `index.ts`
- tables use uuid PKs with `defaultRandom()` (except auth tables which use text PKs)
- snake_case in DB, camelCase in JS; timestamps always `created_at` + `updated_at`
- relations defined in same file as tables, named relation keys for disambiguation
- circular imports between schema files are fine (drizzle relations are lazy)
- auth `userRelations` in `auth.ts` references tables from other schema files

---

### Task 1: add core vault schema tables and enums — DONE
- created `packages/db/src/schema/vaults.ts` with 3 enums (vault_type, vault_membership_role, vault_invitation_status) and 3 tables (vault, vault_membership, vault_invitation)
- vault slug is unique, invitation dedup uses partial unique index on (vault_id, email) WHERE status='pending'
- added vault membership/invitation relations to user in auth.ts
- `bun run check-types` and `bun run check` pass

### Task 2: add vault ownership columns to skills schema — DONE
- added `ownerVaultId` (uuid, nullable FK to vault.id, onDelete cascade) to skill table in `packages/db/src/schema/skills.ts`
- nullable for now — task 3 backfill migration will make it non-null
- added `skill_owner_vault_id_idx` index and `skill_vault_slug_idx` partial unique index on (owner_vault_id, slug) WHERE owner_vault_id IS NOT NULL
- added `ownerVault` one-relation in skillRelations and `skills` many-relation in vaultRelations
- kept legacy `ownerUserId` column and its indexes intact for migration compatibility
- `bun run check-types` and `bun run check` pass

### Task 3: create migration to backfill personal vaults — DONE
- generated migration 0007: creates vault tables + enums + ownerVaultId column (schema from tasks 1&2 was applied via db:push, this captures it as a migration)
- 0007 also includes backfill SQL: creates personal vault per user (slug=`personal-<userId>`), owner membership rows, and updates skill.owner_vault_id from user's personal vault
- generated migration 0008: makes owner_vault_id NOT NULL and rebuilds skill_vault_slug_idx without the partial WHERE clause
- updated schema: `ownerVaultId` is now `.notNull()`, removed partial index WHERE clause
- fixed type errors in `packages/api/src/routers/skills.ts` (create mutation) and `packages/auth/src/default-skills.ts` (seedDefaultSkillsForUser) — both now look up the user's personal vault before inserting skills
- `bun run check-types` and `bun run check` pass

### Task 4: bootstrap global system-default vault and memberships — DONE
- created `packages/auth/src/system-default-vault.ts` with 4 idempotent helpers:
  - `ensureSystemDefaultVault()` — creates or returns the single system_default vault (slug `system-default`, isSystemManaged=true)
  - `backfillSystemDefaultVaultMemberships()` — ensures all existing users have member role on the default vault, batched inserts with onConflictDoNothing
  - `attachUserToSystemDefaultVault(userId)` — attaches a single new user to the default vault
  - `ensurePersonalVault(userId, userName)` — creates personal vault + owner membership for new users (was missing from signup flow — migration 0007 only handled existing users)
- updated `packages/auth/src/index.ts` user creation hook: now creates personal vault → attaches to system-default vault → seeds default skills (in order, since seedDefaultSkillsForUser needs the personal vault to exist)
- updated `packages/auth/src/sync-default-skills.ts` to call `backfillSystemDefaultVaultMemberships()` before syncing default skills — runs on every deploy/build so new users who slipped through also get attached
- no migration needed: vault creation + membership backfill is runtime-idempotent via the sync entrypoint
- `bun run check-types` and `bun run check` pass

### Task 5: replace per-user default skill seeding with default-vault sync — DONE
- removed `seedDefaultSkillsForUser` from `packages/auth/src/default-skills.ts` and its call from the user creation hook in `packages/auth/src/index.ts`
- replaced `syncDefaultSkillsForAllUsers` with `syncDefaultSkillsToDefaultVault` — upserts templates into the single system-default vault instead of syncing per-user copies
- added `createDefaultSkillInVault` helper for first-time creation of canonical default skills (ownerUserId=null, ownerVaultId=default vault)
- made `skill.ownerUserId` nullable in schema (migration 0009): drops NOT NULL, changes onDelete to SET NULL, removes legacy `skill_private_owner_slug_idx` unique index
- updated skill output zod schema: `ownerUserId: z.string().nullable()`
- deploy/build pipeline unchanged — `sync-default-skills.ts` entrypoint now calls the new `syncDefaultSkillsToDefaultVault`
- idempotency preserved: version hash check skips unchanged templates, unique (ownerVaultId, slug) prevents duplicates
- `bun run check-types` and `bun run check` pass
